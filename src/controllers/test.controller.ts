// import { NextFunction, Request, Response } from 'express';
// // import { CreatePropertyDto } from '@dtos/property.dto';
// import testService from '@services/test.service';

// class TestController {
//     public testService = new testService();
// public testOwner = async (req: Request, res: Response, next: NextFunction): Promise<void> => {
//     try {
//       const findAllOwnerData: any = await this.testService.findAll(req);

//       res.status(200).json({ message: 'List o', data: findAllOwnerData, });
//     } catch (error) {
//       next(error);
//     }
//   };
// }


// export default TestController;